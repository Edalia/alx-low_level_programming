#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 *rev_string - reverse string
 *@s: pointer of param
 *Return: void
 */
void rev_string(char *s)
{
	/*int len = strlen(s);*/

	/*while (len >= 0)
	{
		*s = s[len];
		len--;
		*s++i;

	}*/

	printf("%p", s);
	printf("%p", &s);

}
