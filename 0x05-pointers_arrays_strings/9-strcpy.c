#include "main.h"
#include <string.h>
/**
 *_strcpy - copy src string to dst string
 *@dest: string to paste to
 *@src: string to copy
 *Return: string
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
