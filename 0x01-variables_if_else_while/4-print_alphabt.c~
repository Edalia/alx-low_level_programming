#include <stdio.h>
/**
 *main - Entry point
 *
 *Description: print alphabet except q,e
 *
 * Return: 0
 */

int main(void)
{
  char c;

  for(c = 'a'; c <= 'z'; c++)
  {
    if(c !='q' && c!='e')
      putchar(c);
  }

  return 0;
}
