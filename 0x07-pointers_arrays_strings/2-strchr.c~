#include "main.h"
#include <string.h>
/**
*_strchr - check character in string
*@s: string to check in
*@c: character to locate 
*Return: char (character address)
*/
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
